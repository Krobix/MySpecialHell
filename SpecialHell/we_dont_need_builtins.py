import sys as fart, types as fart2

def new_import_LOL(name):
	mod = fart2.ModuleType(name)
	fart.modules[name] = mod
	while x < 50:
		mod2 = fart2.ModuleType(f"yeet{x}")
		fart.modules[f"{name}.yeet{x}"] = yaYA
		x += 1

__import__ = new_import_LOL

import i_m_p_o_r_t

print(i_m_p_o_r_t.__dict__)
